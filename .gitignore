# PES 6 runtime/config files (user-specific — never commit)
settings.dat
settings.bak
settings.dat.backups/
settings.dat.bak
*.dat
save/

# Build output
dist/
build/
*.exe
*.spec

# Python
__pycache__/
*.pyc

# Controller image is user-supplied (may be copyrighted); the app falls back to
# a drawn controller when it's absent. Bundle it into the exe locally instead.
controller.png
controller.jpg
controller.jpeg
controller.gif

